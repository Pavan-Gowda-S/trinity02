# Trinity Predictive Insight

Import and fully inspect this existing GitHub repo: https://github.com/Pavan-Gowda-S/trinity01.git

Do NOT rebuild from scratch. Preserve all working routes, components, data, portals and styling; modify the existing code in-place and implement the missing functionality after auditing the repo.

Upgrade TRINITY into a polished, demo-ready SIH 2026 predictive cybercrime intelligence prototype:

Fix architecture wording: rename Citizen to “NCRP / Citizen”. Make it explicit that citizens report through NCRP; TRINITY is only the predictive intelligence layer and does NOT replace NCRP, I4C, CFCFRMS, banks or police systems.

Make the core demo workflow genuinely functional using only synthetic data:
Case → transaction analysis → connected/split money-flow → historical pattern matching → time analysis → geographic proximity → ATM clustering → transparent risk scoring → ranked clusters → WHERE + WHEN prediction → nearest police station → actionable alert → audit log.
Add/repair “Run Prediction” so this pipeline visibly executes progressively and dynamically updates the case, prediction and alerts instead of relying only on precomputed static results. Keep the model clearly labeled “Prototype Predictive Model”; never claim real government data or ML training.

Preserve and improve the existing Dashboard, Cases, Case Intelligence, Money Flow, Predictions, Heatmap, Alerts, Reports, Bank/FI, I4C and Admin portals. Keep role-based visibility and synthetic/masked data.

Add a clear “← Back” control to every internal/detail page, returning to the actual previous context and preserving filters/search/selected case where practical.

Upgrade the global visual identity: huge faint GREEN “TRINITY” across the entire background behind content, never as a navbar/logo; low opacity + subtle glow; many tiny green particles gently orbit/roam like atoms with varied slow orbital paths, slight depth/parallax; the TRINITY word itself continuously and extremely slowly rotates 360°. Keep everything elegant, calm, premium, government-grade and readable—no excessive neon/cyberpunk, flashing or distracting animation.

Keep Bengaluru as the primary synthetic demo geography and retain the existing believable datasets. Ensure new complaints/predictions use the existing store architecture and remain internally consistent.

Before finishing, audit every route and interaction for broken navigation, dead buttons, inconsistent role labels, static prediction behavior and visual regressions. Fix only what is necessary. Do not remove existing working features or add unrelated features.

Goal: a coherent, functional, visually impressive SIH jury demo—not a collection of static screens.

This project was built with [Lovable](https://lovable.dev).

**Live app**: https://trinity02.lovable.app

## Build with Lovable

Continue developing this project in the [Lovable editor](https://lovable.dev/projects/bc8113b3-ee74-49a7-bd20-2a3ba306f867).

- **Ship faster**: describe what you want to build and Lovable handles the code.
- **Stay in sync**: every change made in Lovable is committed straight to this repository.
- **Full ownership**: this code is yours. Push to `main` on GitHub and your changes sync back into Lovable, ready for your next prompt.

## Development

Prefer working locally? You need Node.js and npm — [install with nvm](https://github.com/nvm-sh/nvm#installing-and-updating).

```sh
git clone <this-repository-url>
cd <repository-name>
npm i
npm run dev
```
